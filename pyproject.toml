[project]
name = "py_data_transfer"
description = "Will follow shortly"
readme = "README.md"
authors = [{name = "felixblochwitz", email = "felix.blochwitz@gmail.com"}]
requires-python = ">=3.12"
version = "1.0.0"

[tool.semantic_release]
assets = []
build_command_env = []
commit_message = "{version}\n\nAutomatically generated by python-semantic-release"
#commit_parser = "angular"
logging_use_named_masks = false
allow_zero_version = false
major_on_zero = true
#repo_dir = "/Users/fblochwitz/py_data_transfer"
no_git_verify = false
tag_format = "v{version}"
version_toml = [
    "pyproject.toml:project.version",
]

[tool.semantic_release.branches.main]
match = "(main|master)"
prerelease_token = "rc"
prerelease = false

[tool.semantic_release.changelog]
template_dir = "templates"
changelog_file = "CHANGELOG.md"
exclude_commit_patterns = []

[tool.semantic_release.changelog.environment]
block_start_string = "{%"
block_end_string = "%}"
variable_start_string = "{{"
variable_end_string = "}}"
comment_start_string = "{#"
comment_end_string = "#}"
trim_blocks = false
lstrip_blocks = false
newline_sequence = "\n"
keep_trailing_newline = false
extensions = []
autoescape = true

[tool.semantic_release.commit_author]
env = "GIT_COMMIT_AUTHOR"
default = "semantic-release <semantic-release>"

[tool.semantic_release.commit_parser_options]
allowed_types = ["major", "MAJOR", "minor", "MINOR", "patch", "PATCH"]
major_types = ["major", "MAJOR"]
minor_types = ["minor", "MINOR"]
patch_types = ["patch", "PATCH"]
default_bump_level = 0

[tool.semantic_release.remote]
name = "origin"
type = "github"
ignore_token_for_push = false
insecure = false

[tool.semantic_release.publish]
dist_glob_patterns = ["dist/*"]
#upload_to_vcs_release = true

